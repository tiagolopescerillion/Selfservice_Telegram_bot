spring:
  security:
    oauth2:
      client:
        registration:
          rhsso:
            client-id: telegram-client
            client-secret: your-secret
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/rhsso"
        provider:
          rhsso:
            issuer-uri: https://testlinux02.testing.cerillion.com:62443/auth/realms/yourrealm
